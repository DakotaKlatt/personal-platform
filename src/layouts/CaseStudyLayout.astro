---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  canonical?: string;
}
const { title, description, pubDate, canonical } = Astro.props;
const formattedDate = pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
---

<BaseLayout title={title} description={description} canonical={canonical}>
  <a href="#main-content" class="skip-link">Skip to main content</a>
  <Header />
  <main id="main-content" class="min-h-screen" tabIndex={-1}>
    <article class="mx-auto max-w-3xl px-6 py-section">
      <header class="mb-block">
        <a href="/case-studies" class="text-sm text-accent hover:underline mb-4 inline-block">← Case studies</a>
        <h1 class="font-display font-semibold text-display-xl text-[var(--color-text)] mt-2">{title}</h1>
        <time datetime={pubDate.toISOString()} class="text-sm text-[var(--color-text-muted)]">{formattedDate}</time>
      </header>
      <div class="prose-custom">
        <slot />
      </div>
    </article>
  </main>
  <Footer />
</BaseLayout>
